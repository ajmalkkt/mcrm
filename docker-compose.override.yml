version: '3.8'
services:
  backend:
    environment:
      - NODE_ENV=development
    ports:
      - "4000:4000"

  frontend:
    environment:
      - NODE_ENV=development
    ports:
      - "3000:3000"
